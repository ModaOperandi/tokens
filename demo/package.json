{
  "name": "moda-themes-demo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "parcel build index.html --out-dir ../docs --public-url '.' --out-file index",
    "dev": "yarn export && parcel index.html --open",
    "export": "node export.js",
    "prebuild": "rm -rf ../docs && yarn export"
  },
  "devDependencies": {
    "node-sass": "^4.12.0",
    "node-sass-export": "^0.0.3",
    "parcel": "^1.12.3",
    "sass": "^1.22.3"
  },
  "dependencies": {
    "chroma-js": "^2.0.4",
    "moda-themes": "^0.0.4",
    "nearest-color": "^0.4.4"
  }
}
